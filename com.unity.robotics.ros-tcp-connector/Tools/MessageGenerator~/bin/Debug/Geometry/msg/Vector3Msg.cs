//Do not edit! This file was generated by Unity-ROS MessageGeneration.
using System;
using System.Linq;
using System.Collections.Generic;
using System.Text;
using Unity.Robotics.ROSTCPConnector.MessageGeneration;

namespace RosMessageTypes.Geometry
{
    [Serializable]
    public class Vector3Msg : Message
    {
        public const string k_RosMessageName = "geometry_msgs/Vector3";
        public override string RosMessageName => k_RosMessageName;

        //  This represents a vector in free space. 
        //  It is only meant to represent a direction. Therefore, it does not
        //  make sense to apply a translation to it (e.g., when applying a 
        //  generic rigid transformation to a Vector3, tf2 will only apply the
        //  rotation). If you want your data to be translatable too, use the
        //  geometry_msgs/Point message instead.
        public double x;
        public double y;
        public double z;

        public Vector3Msg()
        {
            this.x = 0.0;
            this.y = 0.0;
            this.z = 0.0;
        }

        public Vector3Msg(double x, double y, double z)
        {
            this.x = x;
            this.y = y;
            this.z = z;
        }

        public override int AddSerializationStatements(int curOffset, List<byte[]> listOfSerializations)
        {
            curOffset = AddAlignment(curOffset, sizeof(double), listOfSerializations);
            listOfSerializations.Add(BitConverter.GetBytes(this.x));
            curOffset += sizeof(double);

            curOffset = AddAlignment(curOffset, sizeof(double), listOfSerializations);
            listOfSerializations.Add(BitConverter.GetBytes(this.y));
            curOffset += sizeof(double);

            curOffset = AddAlignment(curOffset, sizeof(double), listOfSerializations);
            listOfSerializations.Add(BitConverter.GetBytes(this.z));
            curOffset += sizeof(double);

            return curOffset;
        }

        public override int Deserialize(byte[] data, int offset)
        {
            offset += Alignment(offset,  8);
            this.x = BitConverter.ToDouble(data, offset);
            offset += 8;
            offset += Alignment(offset,  8);
            this.y = BitConverter.ToDouble(data, offset);
            offset += 8;
            offset += Alignment(offset,  8);
            this.z = BitConverter.ToDouble(data, offset);
            offset += 8;

            return offset;
        }

        public override string ToString()
        {
            return "Vector3Msg: " +
            "\nx: " + x.ToString() +
            "\ny: " + y.ToString() +
            "\nz: " + z.ToString();
        }
    }
}
