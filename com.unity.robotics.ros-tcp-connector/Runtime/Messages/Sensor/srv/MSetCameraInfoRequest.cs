//Do not edit! This file was generated by Unity-ROS MessageGeneration.
using System;
using System.Linq;
using System.Collections.Generic;
using System.Text;
using Unity.Robotics.ROSTCPConnector.MessageGeneration;

namespace RosMessageTypes.Sensor
{
    public class MSetCameraInfoRequest : Message
    {
        public const string RosMessageName = "sensor_msgs/SetCameraInfo";

        //  This service requests that a camera stores the given CameraInfo 
        //  as that camera's calibration information.
        // 
        //  The width and height in the camera_info field should match what the
        //  camera is currently outputting on its camera_info topic, and the camera
        //  will assume that the region of the imager that is being referred to is
        //  the region that the camera is currently capturing.
        public MCameraInfo camera_info;
        //  The camera_info to store

        public MSetCameraInfoRequest()
        {
            this.camera_info = new MCameraInfo();
        }

        public MSetCameraInfoRequest(MCameraInfo camera_info)
        {
            this.camera_info = camera_info;
        }
        public override List<byte[]> SerializationStatements()
        {
            var listOfSerializations = new List<byte[]>();
            listOfSerializations.AddRange(camera_info.SerializationStatements());

            return listOfSerializations;
        }

        public override int Deserialize(byte[] data, int offset)
        {
            offset = this.camera_info.Deserialize(data, offset);

            return offset;
        }

        public override string ToString()
        {
            return "MSetCameraInfoRequest: " +
            "\ncamera_info: " + camera_info.ToString();
        }
    }
}
